# simple

This repository includes an imported snapshot of the `midlevels/startmeup` app in [`startmeup/`](./startmeup/).

To run the imported app locally:

1. `cd startmeup`
2. `npm install`
3. `npm start`
